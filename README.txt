Flags:

-o [filename]
--output [filename]
Runs code on test data and outputs submission file at filename

-r
Regenerates the test data. This happens either way if it's your first time
running with -o.
